if( $('#main-content-seminar-lection')[0] )
{

    // var disqus_shortname generated by Laravel.

    if ( disqus_shortname !== null )
    {

        /*
        * Update message resource form.
        */

        // var sequence_content generated by Laravel.
        if( !sequence_content )
        {
            $('#sequence-new-modal').modal('show');
        }



        /*
        * DISQUS implementation.
        */
        // var disqus_identifier generated by Laravel.
        var disqus_config = function () {
            this.page.identifier = disqus_identifier;
        };

        (function() {  // REQUIRED CONFIGURATION VARIABLE: EDIT THE SHORTNAME BELOW
            var d = document, s = d.createElement('script');

            s.src = '//' + disqus_shortname + '.disqus.com/embed.js';

            s.setAttribute('data-timestamp', +new Date());
            (d.head || d.body).appendChild(s);
        })();

    }


    /*
    * Experimental auto pause and auto redirect code.
    */

    // @TODO: Implement autoredirect standard!

    // This code have to be refactored (storing and dynamic generation).
	var paused = false;

	/* Pausierfunktion */
	var autoPause = function ( timepoint ) {
		if( synthesise_player.currentTime() >= timepoint && synthesise_player.currentTime() <= (timepoint + 0.25) && !paused )
		{
			$( '#infomessage' ).show();
			synthesise_player.pause();
			paused = true;
		}
	};

    var autoRedirect = function () {

        synthesise_player.on( 'ended', function () {

            var new_url = window.location.href.slice(0, -1) + ( parseInt(sequence) + 1);

            swal({
                title: "Sequenz abgeschlossen!",
                text: "Sie können direkt mit der nächsten fortfahren.",
                icon: "success",
                timer: 3000,
                buttons: ["Hier bleiben.", "Ja, weitermachen!"]
            }).
            then(() => {

                window.location = new_url;

            });

        });

    };

    // var lection_name generated by Laravel.
    // var sequence generated by Laravel.

	// Woher kommen Zahlen
	if ( lection_name === 'Zahldarstellungen und Stellenwertsysteme' && sequence == 1 )
	{
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
    }

    // Das römische Zahlzeichensystem
	if ( lection_name === 'Zahldarstellungen und Stellenwertsysteme' && sequence == 2 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
        }


    // Bündeln
	if ( lection_name === 'Zahldarstellungen und Stellenwertsysteme' && sequence == 3 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
		}


	// Stellenwertschreibweise
	if ( lection_name === 'Zahldarstellungen und Stellenwertsysteme' && sequence == 4 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
	    }


	// g-adische Systeme
	if ( lection_name === 'Zahldarstellungen und Stellenwertsysteme' && sequence == 5 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
		}


	// SUBTRAKTION IN STELLENWERTSYSTEMEN
	if ( lection_name === 'Zahldarstellungen und Stellenwertsysteme' && sequence == 6 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
				});
			});
	}

    // STELLENWERTSYSTEME
	if ( lection_name === 'Addition und Subtraktion' && sequence == 1 )
	{
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// BINÄRSYSTEM
						autoPause(179);

						// AUFTRAG BINÄRSYSTEM
						autoPause(200);

						// DUODEZIMALSYSTEM
						autoPause(236);
					});
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
    }

    // ZAHLSYSTEMWECHSEL
	if ( lection_name === 'Addition und Subtraktion' && sequence == 2 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// WECHSEL
						autoPause(80);

						// G-ADISCHE DARSTELLUNG
						autoPause(159);

					});
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
        }


    // ZAHLNAMENSGEBUNG
	if ( lection_name === 'Addition und Subtraktion' && sequence == 3 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// SPRACHKONVENTIONEN
						autoPause(52);
					});
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
		}


	// INTERPRETATION DER STELLENWERTSCHREIBWEISE
	if ( lection_name === 'Addition und Subtraktion' && sequence == 4 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
	    }


	// ADDITION IN STELLENWERTSYSTEMEN
	if ( lection_name === 'Addition und Subtraktion' && sequence == 5 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// ADDIEREN HEXADEZIMALSYSTEM ERGEBNIS
						autoPause(73);

						// STELLENWEISE ADDIEREN
						autoPause(243);

						// SCHRITTWEISE ADDIEREN
						autoPause(273);

						// GEGENSINNIGES VERÄNDERN
						autoPause(302);

						// BEISPIEL 1
						autoPause(313);

						// BEISPIEL 2
						autoPause(337);

						// BEISPIEL 3
						autoPause(357);

						// OBJEKTEBENE
						autoPause(430);

						// STELLENWERTTAFEL
						autoPause(468);

						// STELLENSCHREIBWEISE
						autoPause(495);
					});
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
		}


	// SUBTRAKTION IN STELLENWERTSYSTEMEN
	if ( lection_name === 'Addition und Subtraktion' && sequence == 6 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// AUFGABE 1
						autoPause(8);

						// AUFGABE 2
						autoPause(20);

						// AUFGABE 3
						autoPause(31);

						// ENDE INTRO
						autoPause(100);

						// STELLENWEISE SUBTRAHIEREN
						autoPause(134);

						// SCHRITTWEISE SUBTRAHIEREN
						autoPause(151);

						// GEGENSINNIGES VERÄNDERN
						autoPause(191);

						// ÜBERTRAG DURCH ENTBÜNDELN
						autoPause(265);

						// ÜBERTRAG DURCH AUFFÜLLEN
						autoPause(307);

						// ÜBERTRAG DURCH ERWEITERN
						autoPause(335);

						// SCHWIERIGKEITEN
						autoPause(378);
					});
				});
			});
	}


	// Analyse der Multiplikation
	if ( lection_name === 'Multiplikation und Division' && sequence == 1 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// KLEINES EINMALEINS
						autoPause(170);

						// KLEINES EINMALEINS ZWEITE PAUSE
						autoPause(255);

						// MUSTER
						autoPause(328);

						// HEXADEZIMALES EINMALEINS
						autoPause(387);

						// HEXADEZIMALES EINMALEINS AUFLÖSUNG
						autoPause(390);

						// QUADRATZAHLEN
						autoPause(427);

						// QUADRATZAHLEN HINWEIS
						autoPause(433);

						// 6ER SYSTEM
						autoPause(511);

					});
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
        }


	// Schriftliche Multiplikation
	if ( lection_name === 'Multiplikation und Division' && sequence == 2 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// MALKREUZ
						autoPause(55.5);

					});
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
		}


	// Der Divisionsalgorithmus
	if ( lection_name === 'Multiplikation und Division' && sequence == 3 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// MONTESSORI
						autoPause(91);

						// ALGORITHMUS
						autoPause(196);

					});
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
        }


	// Division nach Adam Ries
	if ( lection_name === 'Multiplikation und Division' && sequence == 4 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

					});
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
		}


	// Division im Hexadezimalsystem
	if ( lection_name === 'Multiplikation und Division' && sequence == 5 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// AUFGABE
						autoPause(26.5);

						// Lösung
						autoPause(149);

					});
					/* SEQUENZ AUTOMATISCH WECHSELN */
					autoRedirect();
				});
			});
		}

	// Analyse Polynomdivision
	if ( lection_name === 'Multiplikation und Division' && sequence == 6 )
	{
			/* Checken, ob der Player gestartet wurde */
			$( document ).ready( function () {
				$( '.vjs-paused, .vjs-control-bar' ).click( function () {
					$( '#infomessage' ).hide();
					paused = false;
				});
				$( '.vjs-poster, .vjs-big-play-button' ).click( function () {
					/* Checkt, ob der Player läuft */
					synthesise_player.on( 'timeupdate', function () {
						// Autopausen definieren

						// UMKEHRUNG
						autoPause(58);

						// ENDE
						autoPause(105);

					});
				});
			});
		}

}
